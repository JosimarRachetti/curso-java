package curso;

public class NotacaoPonto {
    public static void main(String[] args) {
        String palavra = "Bem vindo ao programa";
        palavra = palavra.replace("ao","no"); //metodo que substitui Substitui ao por no
        palavra = palavra.toUpperCase(); //Metodo que torna todas as letras maisculas
        palavra = palavra.concat("..."); //metodo que une duas frases
    // tipos primitivos não possuem notação ponto"."

    
    }
}
